.row
  .col-md-8

    %p#notice= notice

    %h1
      = @product.name
    %p
      = @product.description

.row
  .col-md-10
    - if @product.images.attached? && @product.images.first.representable?
      = image_tag @product.images.first.representation(resize_to_limit: [600, 600]), class: 'img-fluid rounded float-right'



.row
  .col-md-8


    %p
      %b Price:
      = number_to_currency(@product.price) if @product.price.present?
    = button_to "Buy", add_to_cart_path(product_id: @product.id), class: 'btn btn-primary btn-lg'

.row
  .col-md-8
    %br
    - if signed_in?
      = link_to 'Edit', edit_product_path(@product)
    \|
    = link_to 'Back', products_path
