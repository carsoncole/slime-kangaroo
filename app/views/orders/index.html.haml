%h1 Orders

%table.table
  %thead
    %tr
      %th Date
      %th Amount
      %th Received
      %th Shipped
      %th Cancelled
      %th Refunded
      %th Email

      %th
      %th
      %th

  %tbody
    - @orders.each do |order|
      %tr
        %td= order.date
        %td= order.amount
        %td= order.created_at
        %td= order.shipped_at
        %td= order.cancelled_at
        %td= order.refunded_at
        %td= order.email
        %td= link_to 'Show', order
        %td= link_to 'Edit', edit_order_path(order)
        %td= link_to 'Destroy', order, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Order', new_order_path
